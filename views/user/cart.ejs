<%- include('../user/userHeader.ejs') %>
 <!-- Navbar Start -->
 <%- include('../user/userNavbar.ejs') %>
 <!-- Navbar End -->

    <!-- Cart Start -->
    <div id="re-load">
    <div class="container-fluid pt-5">
        <div class="row px-xl-5">
            <div class="col-lg-8 table-responsive mb-5">
                <table class="table table-bordered text-center mb-0">
                    <thead class="bg-secondary text-dark">
                        <tr>
                            <th>Products</th>
                            <th>Price</th>
                            <th>Quantity</th>
                            <th>Total</th>
                            <th>Remove</th>
                        </tr>
                    </thead>
                    <tbody class="align-middle">
                        
                     
                            <%carts?.products?.forEach(function(product,index){ %>
                                <tr>
                                    <td class="d-flex px-2 mx-2 align-middle"><img class="px-2" src="/product/img/<%= product.productId.productImage[0] %>" alt="" style="width: 50px;"><%= product.productId.productName %></td>
                                    <td class="align-middle">$ <%= product.productPrice%></td>
                                    <td class="align-middle">
                                        <div class="input-group quantity mx-auto" style="width: 100px;">
                                            <div class="input-group-btn">
                                                <button class="btn btn-sm btn-primary btn-minus" onclick="changeQuantity('<%= product.productId._id%>',-1)" >
                                                <i class="fa fa-minus"></i>
                                                </button>
                                            </div>
                                            <input type="text" class="form-control form-control-sm bg-secondary text-center" value="<%=product.count%>" >
                                            <div class="input-group-btn">
                                                <button class="btn btn-sm btn-primary btn-plus" onclick="changeQuantity('<%= product.productId._id%>',1)">
                                                    <i class="fa fa-plus"></i>
                                                </button>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="align-middle">$ <%= product.totalPrice %></td>
                                    <td class="align-middle"><button class="btn btn-sm btn-primary" onclick="removeProduct('<%= product.productId._id %>')"><i class="fa fa-trash"></i></button></td>
                                </tr>
                           
                       <% })%>
                        
                        
                    </tbody>
                </table>
            </div>
            <div class="col-lg-4">
                <form class="mb-5" action="">
                    <div class="input-group">
                        <input type="text" class="form-control p-4" placeholder="Coupon Code">
                        <div class="input-group-append">
                            <button class="btn btn-primary">Apply Coupon</button>
                        </div>
                    </div>
                </form>
                <div class="card border-secondary mb-5">
                    <div class="card-header bg-secondary border-0">
                        <h4 class="font-weight-semi-bold m-0">Cart Summary</h4>
                    </div>
                    <div class="card-body">
                        <div class="d-flex justify-content-between mb-3 pt-1">
                            <h6 class="font-weight-medium">Subtotal</h6>
                            <h6 class="font-weight-medium">$150</h6>
                        </div>
                        <div class="d-flex justify-content-between mb-3 pt-1">
                            <h6 class="font-weight-medium">Shipping</h6>
                            <h6 class="font-weight-medium">$10</h6>
                        </div>
                        <div class="d-flex justify-content-between">
                            <h6 class="font-weight-medium">Discount</h6>
                            <h6 class="font-weight-medium">$0</h6>
                        </div>
                    </div>
                    <div class="card-footer border-secondary bg-transparent">
                        <div class="d-flex justify-content-between mt-2">
                            <h5 class="font-weight-bold">Total</h5>
                            <h5 class="font-weight-bold">$160</h5>
                        </div>
                        <button class="btn btn-block btn-primary my-3 py-3">Proceed To Checkout</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
    <!-- Cart End -->

    <!-- ajax for quantity updation -->
    <script>
        //for changing quantity
        function changeQuantity(productId, count) {
            console.log(productId,count)
    $.ajax({
        type: 'post',
        url: '/cartModify',
        data: {
            productId: productId,
            count: count
        },
        success: function (response) {
            if (response.success) {
                $("#re-load").load("/cart #re-load");
            } else {
                swal.fire({
                    position: "center",
                    icon: "warning",
                    text: response.message,
                    timer: 1500,
                    showConfirmButton: false,
                });
            }
        }
    });
}

function removeProduct(productId){
            $.ajax({
                url : '/removeProduct',
                method : 'post',
                data : {
                    productId : productId
                },
                success : function(response){
                    if(response.success){
                        $("#re-load").load("/cart #re-load");
                    }
                }
                
            })
         }

    </script>
    
    <hr style="border: 0px solid #787878; margin: 100px 0;">
    <%- include('../user/userFooter.ejs') %>